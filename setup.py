from setuptools import setup, find_packages
setup(
    name='distortion',
    version='0.1.0',
    packages=[ 
                'common',
                'simulation',
                'distortion_correction',]
)